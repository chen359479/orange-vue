const state = {
    allMenuList:[]
};

const mutations = {
    setAllMenuList: (state, data) => {
        state.allMenuList = data;
    },
}
const actions = {};

export default {
    state,
    mutations,
    actions
};
