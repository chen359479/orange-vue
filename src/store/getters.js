const getters = {
    userInfo: state => state.user.userInfo,
    userJit: state => state.user.userJit,
    userState:state => state.user.userState,
    allMenuList:state => state.menu.allMenuList,
}
export default getters
